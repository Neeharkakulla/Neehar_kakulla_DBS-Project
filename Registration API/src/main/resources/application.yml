spring:
  application.name: RegistrationAPI
  datasource:
    url: jdbc:mysql://localhost:3306/dbsRegistration?createDatabaseIfNotExist=true
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect